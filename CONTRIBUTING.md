# Contributing

Run `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`, and `npm run community:check` before opening a PR. Keep the plugin public-API-only: do not patch native File Explorer internals or copy Obsidian source.
