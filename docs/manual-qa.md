# Manual QA

Use the Windows-local `obsidian-test-vault`. Run `npm run sync:test-vault` and `npm run smoke:cdp`. Confirm the separate Folder Sort View opens, folders sort Z-to-A, and files open normally.
