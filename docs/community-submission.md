# Community Submission

Submit this repository URL:

```text
https://github.com/viggomeesters/obsidian-folder-sort-view
```

Before submission, verify release assets include `main.js`, `manifest.json`, and `styles.css`, and `npm run community:check` passes.
